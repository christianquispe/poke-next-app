import { globalCss } from "@nextui-org/react";

export const globalStyles = globalCss({
  body: {
    background: "linear-gradient(115deg, $gray100 57.7%, $black 57.7%)",
    backgroundRepeat: "no-repeat",
    height: "100vh",
    overflow: "hidden",
  },
});
